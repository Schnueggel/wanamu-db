box: postgres
  env:
    PGPASSWORD: $DB_PASSWORD
build:
  steps:
    - script:
      name: make dirs
      code: |
        mkdir /var/lib/postgresql/dump
        mkdir /scripts
        cp scripts/initdb.sh /docker-entrypoint-initdb.d/
    - script:
      name: test db
      code: |
        psql -d $DB_NAME -U $DB_USER -c <<EOF
        \dt *.
        EOF
