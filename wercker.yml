box:
  id: postgres
  env:
    PGPASSWORD: $DB_PASSWORD
build:
  steps:
    - script:
      name: make dirs
      code: |
        echo $PG_MAJOR
        mkdir /var/lib/postgresql/dump
        cp ./scripts/initdb.sh /docker-entrypoint-initdb.d/
    - script:
          name: start
          code: |
            /docker-entrypoint.sh postgres --help


