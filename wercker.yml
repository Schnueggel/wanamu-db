box:
  id: postgres
  env:
    PGPASSWORD: $DB_PASSWORD
build:
  steps:
    - script:
      name: make dirs
      code: |
        mkdir /var/lib/postgresql/dump
        mkdir /scripts
        cp ./scripts/dump.sh /scripts/
        cp ./scripts/initdb.sh /docker-entrypoint-initdb.d/
    - script:
          name: start
          code: |
            postgres
    - script:
      name: test db
      code: |
        echo $DB_NAME
        psql -d $DB_NAME -U $DB_USER <<EOF
        \dt *.
        EOF
